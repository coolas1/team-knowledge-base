## 1. B1 范围与基线（memory-scope；无前置）

- [x] 1.1 建立至少 40 个固定标注案例及上游/TKB 运行清单，记录 SHA、模型、配置与数据指纹；验证 design 中每类至少 3 个案例并能重复加载。
- [x] 1.2 在 types/protocols/interface 增加默认兼容的 MemoryScope 和标签表达式；验证旧请求不带新字段仍成功、非法匹配模式报错。
- [x] 1.3 为 bank、记忆、实体、关系及现有任务/模型添加范围迁移与唯一约束；验证旧数据 default-team 回填、断点续跑及计数一致。
- [x] 1.4 在 MCP/BFF 解析可信范围和允许标签，贯穿 service/repository 所有读写；验证请求伪造范围无法扩大可见数据。
- [x] 1.5 更新 Neo4j 投影及图搜索的范围属性和过滤；验证同名实体跨 bank 不产生跨范围结果。
- [x] 1.6 实现标签 any/all/strict/exact 和组合匹配，区分 session 来源标签与归纳 write scope；用包含无标签的真值表测试验证。
- [x] 1.7 增加范围级策略版本存储及功能开关依赖检查；验证默认配置兼容和错误启用组合被拒绝。
- [x] 1.8 完成 B1 数据库/图隔离、迁移回退及旧 API 契约验收，记录测试命令、结果和待验证项；通过后才启用隔离范围。
- [x] 1.9 将 documents/chunks 纳入范围迁移并让文件导入及后台重入库保留归属；验证旧数据 default-team、不同范围相同内容不错误复用。
- [x] 1.10 文档列表、详情、原文、编辑、删除及 MCP/BFF 入口应用可信范围；验证知道其他范围文档 ID 也不可访问或修改。
- [x] 1.11 普通 GraphRAG 召回、关联文档及图描述按来源范围隔离；验证关闭记忆查询后的 fallback 同样不泄漏。任务 1.8 验收须在 1.9–1.11 完成后执行。

- [x] 1.12 Pi 会话生命周期按可信范围隔离，所有会话 MCP 工具、自动召回和投递继承范围；验证跨范围拒绝、重启/凭据轮换、旧 default-team 及删除后遗忘。任务 1.8 在本项通过后验收。

## 2. B2 可靠保留（memory-retention、conversation-memory；前置 B1）

- [x] 2.1 在完成轮次的持久化边界增加 delivery intent 和稳定 scope/session/turn 键；验证保存后进程退出仍可发现未投递轮次。
- [x] 2.2 实现 Pi 补投扫描、durable acceptance 确认和内容冲突处理；验证接收后确认前崩溃不产生重复，未完成轮次不入队。
- [x] 2.3 添加关联 operation 及阶段结果字段，接入 queue/worker 租约 fencing、重试和取消；验证过期 worker 不覆盖新状态。
- [x] 2.4 扩展 retain 的 Agent/说话者、来源时间、时区及策略版本输入；验证用户第一人称、Agent 建议、实际行动和补录相对时间案例。
- [x] 2.5 分离 success/empty/degraded/failed 抽取结果并增加阶段重处理入口；验证 LLM 超时及非法 JSON 不返回完整成功。
- [x] 2.6 实现范围内实体候选、别名和上下文消歧；验证别名关联及同名不同人分立。
- [x] 2.7 实现误合并纠正及关系重建；验证纠正后各实体事实归属恢复且原始名称保留。
- [x] 2.8 增加文档 revision、请求幂等键和稳定 chunk/fact 引用；验证同键不同内容冲突、并发 revision 冲突。
- [x] 2.9 实现 append/replace 增量内容识别及策略版本缓存失效；验证重试只追加一次、未变化内容不再调用抽取器。
- [x] 2.10 完成 B2 网络失败/重启/租约故障及抽取语义对照验收；输出批次报告，保留聊天失败隔离与旧调用兼容。

## 3. B3 持续归纳（memory-consolidation；前置 B1+B2）

- [x] 3.1 添加 observation 版本、证据依赖和待归纳事实版本迁移；验证从旧单次 observation 回填和恢复游标。
- [x] 3.2 实现事实提交与 consolidation outbox 原子性、范围任务合并及 worker 恢复；验证事务回滚无幽灵任务且重启不漏事实。
- [x] 3.3 实现相关 observation/源事实读取及动作 schema 验证；验证不能更新未读到或其他范围的 observation。
- [x] 3.4 实现 create/update/delete 原子提交、版本检查和处理游标；验证两个并发 worker 不丢更新或重复应用动作。
- [x] 3.5 实现精确去重、更新目标重复动作合并及证据并集；验证重复创建和同轮多次更新结果稳定。
- [x] 3.6 实现可配置语义近邻加等价判定，覆盖创建和更新；验证改写合并、矛盾不误合并和跨 scope 不合并。
- [x] 3.7 加入历史快照、变化/冲突证据及 stale 状态；验证新偏好与历史偏好查询能够区分。
- [x] 3.8 实现来源删除/替换时派生失效、tombstone、重算及图 outbox；验证并发归纳不会复活遗忘内容，历史也不能展开被删原文。
- [x] 3.9 配置归纳容量、批量、最大轮数、费用/并发限制及旧同步归纳切换；验证达到上限显式排队或报告状态，无双重归纳。
- [x] 3.10 完成 B3 跨三轮归纳、重复、变化、删除竞争和 worker 恢复验收；输出对照指标及灰度回退记录。

## 4. B4 检索与证据（memory-retrieval、conversation-memory；前置 B3）

- [x] 4.1 扩展 Recall 请求类型、时间、include、min_scores、prefer_observations 和预算参数；验证旧参数默认语义兼容及请求不能扩大硬上限。
- [x] 4.2 贯穿四路检索相同过滤和预算，复用 DeadlineBudget/MMR；验证超时降级、各分支过滤及输出 token 上限。
- [x] 4.3 增加 chunk/document expand 接口及来源存在性/范围/有效性检查；验证删除和越权 ID 不返回内容。
- [x] 4.4 返回证据时间、来源依赖和 stale reason；验证变更源使旧综合认识可识别且不可冒充当前证据。
- [x] 4.5 扩展 Pi 注入策略、类型/时间展示和 recall 配置；验证预算、不可信边界及注入内容不回流历史。
- [x] 4.6 完成 B4 契约、多路故障、展开和现有 fast/deep 检索回归；输出批次报告和延迟/用量比较。

## 5. B5 长期模型（mental-models；前置 B3+B4）

- [x] 5.1 添加模型定义、来源、版本和刷新配置迁移及 CRUD API；验证范围隔离和旧模型迁移后可读。
- [x] 5.2 实现手动完整刷新和版本发布，复用 B4 证据检索及现有 providers 综合能力；验证不依赖尚未实现的 B6 工具循环。
- [x] 5.3 实现按相关事实水位的事件触发和任务合并；验证无关范围不触发、同一水位不重复刷新。
- [x] 5.4 实现定时刷新、重启恢复及过期判定；验证错过调度后的补偿和有界重试。
- [x] 5.5 实现结构化 delta 校验、完整刷新回退及失败保留旧版本；验证非法 delta 不覆盖最后成功内容。
- [x] 5.6 接入 B3 来源删除依赖与发布时版本复核；验证刷新期间删除来源不会发布失效内容。
- [x] 5.7 完成 B5 项目概况手动/事件/定时刷新和失败恢复验收；输出版本、证据与成本记录。

## 6. B6 动态 Reflect（memory-reflection；前置 B4+B5）

- [x] 6.1 定义 search_mental_models/search_observations/recall/expand/done 工具契约及适配器；验证每个工具继承受信任范围。
- [x] 6.2 实现依据工具结果迭代规划的状态循环，保留旧路径开关；验证第二轮出现缺口后继续查证，以及无原始 recall 时仍可读取有效 model。
- [x] 6.3 统一 LLM/工具总期限、总 token 和最大迭代限制；验证工具超时或预算耗尽后有限结束并报告证据不足。
- [x] 6.4 实现 stale observation/model 的事实回查规则；验证旧摘要不直接用于当前事实断言。
- [x] 6.5 分离检索证据与 actual citations，校验引用并限制一次修复；验证未知/越权/删除引用被拒绝。
- [x] 6.6 实现范围级 directives CRUD、触发规则和受信任注入；验证普通历史命令不成为指令。
- [x] 6.7 将 B5 模型刷新接入可选新推理器，保持刷新版本发布规则；验证两条推理路径均满足失败保留及来源删除规则。
- [x] 6.8 完成 B6 多跳、过期、空证据、预算和引用对照验收；输出工具轨迹、语义指标及性能记录。

## 7. B7 管理与综合验收（memory-operations；前置 B1-B6）

- [ ] 7.1 提供 operation 列表/详情/重试/取消 API 和 CLI/MCP 入口；验证按 session/turn 贯穿全部阶段且越权操作失败。
- [ ] 7.2 提供事实/来源、observation 历史和实体纠正管理 API；验证已删除来源不可展开且修改可追溯。
- [ ] 7.3 增加前端任务诊断与记忆证据页面；验证失败原因、重试状态和来源链接在组件/API 测试中正确。
- [ ] 7.4 增加范围策略、模型刷新和 directives 管理页面；验证表单校验、范围约束和刷新版本展示。
- [ ] 7.5 完成全量上游/TKB 一轮固定对照（按用户效率要求，失败项才追加定向验证），按 design 阈值输出各类正确率、p50/p95、用量、失败率及未对齐项；未过阈值不勾选完成。
- [ ] 7.6 执行全量迁移、历史补投、回填中断恢复和兼容版本回退演练；验证计数、范围和删除 tombstone 无回归。
- [ ] 7.7 完成项目 lint/pytest、相关真实服务集成测试及前端 npm test，整理按 CI/CD 管线发布的运行手册；记录命令和结果，不能用单元测试替代真实故障验收。
- [ ] 7.8 核对九份规格与全部批次证据，交付最终能力矩阵及明确排除的 SDK/第三方集成兼容后续清单；所有批次验收完成后才允许归档本变更。
