## 1. 建立统一的深度检索截止时间

- [x] 1.1 在 Hindsight 配置中增加深度检索总预算和 query analysis、embedding、retrieval arm、rerank 的阶段预算，采用方案默认值并校验各阶段配置；用配置单元测试验证默认值、覆盖值和非法值。
- [x] 1.2 实现基于单调时钟的总截止时间与阶段剩余时间计算器，定义 `succeeded`、`empty`、`timed_out`、`failed`、`cancelled`、`skipped` 阶段结果；用可控时钟测试预算递减、耗尽和边界行为。
- [x] 1.3 将有效阶段超时传递到所有模型、embedding 和检索外部调用，并在调用端增加异步超时边界；用永久挂起和延迟返回的 fake provider 验证调用会在预算内终止。
- [x] 1.4 统一取消处理，保证 `CancelledError` 不被降级逻辑吞掉，并取消、等待所有并行子任务；用任务泄漏检查验证客户端取消后没有后台检索继续运行。

## 2. 让深度检索在依赖异常时可降级返回

- [x] 2.1 重构 query analysis 与 embedding 阶段，使分析失败时跳过 graph/temporal arm，embedding 失败时仍可执行不依赖向量的检索；用组合故障测试验证可用分支仍会执行。
- [x] 2.2 将 semantic、keyword、graph、temporal 检索臂分别置于阶段预算内，独立记录结果并汇总已成功的证据；用单臂超时、单臂异常和多臂部分成功测试验证不会等待失效分支。
- [x] 2.3 为 rerank 增加候选条数、单条文本长度和总输入量上限，并在 rerank 失败或超时时使用确定性的融合分数排序；用固定语料测试输入边界、稳定顺序和回退结果。
- [x] 2.4 扩展深度检索响应元数据，明确区分正常空结果、降级结果、总超时和全部依赖不可用，同时保持现有请求字段兼容；用 MCP 契约测试验证旧客户端仍可解析响应。
- [x] 2.5 为成功、降级、失败和取消路径记录同一个 correlation ID、阶段耗时与阶段状态，过滤查询正文、证据正文、密钥和连接信息；用日志捕获测试验证字段齐全且不泄露敏感内容。

## 3. 将关键词召回限制在有索引的候选集内

- [x] 3.1 增加由英文词和中文双字词组成的 lexical token 持久化字段，并覆盖 memory unit 的新增与更新写入路径；用中英文及混合文本测试验证写入 token 与现有分词规则一致。
- [x] 3.2 增加可在线执行的 GIN 索引迁移、分批可恢复回填、完整性检查和读路径开关；用数据库集成测试验证回填幂等、断点续跑、并发写入以及完成前不会切换索引读路径。
- [x] 3.3 实现索引重叠查询，只物化配置上限内的候选记录，再在候选集上执行 Python BM25；用大于三万条 memory unit 的测试数据断言物化数量不超过上限且不会全表加载到应用内存。
- [x] 3.4 在冻结的中英文查询集上比较旧关键词检索和新候选预筛选的召回与排序，并记录可接受阈值；运行三万条以上数据的基准测试，确认查询时间和内存满足设计中的发布门槛。

## 4. 协调 Pi 回合预算并提供一次快速兜底

- [x] 4.1 将 Pi 默认深度工具超时改为 60 秒、回合上限改为 180 秒，并增加至少 60 秒的回答预留配置；用启动配置测试验证 `deep timeout + reserve < turn timeout`，非法组合直接报错。
- [x] 4.2 在 Pi 回合中使用单调截止时间计算每次工具调用的有效超时，把回合剩余时间和回答预留纳入限制，并把取消信号传到 MCP 调用；用临近截止时间的测试验证工具不会侵占回答预算。
- [x] 4.3 在受信任的 Pi 工具包装层识别深度检索的类型化超时或无可用证据结果，在预算允许时只调用一次 `tkb_search_fast`，并维护每回合搜索尝试计数；用超时、兜底失败、用户取消和重复请求测试验证无递归重试。
- [x] 4.4 让流式事件和最终回答标明深度检索降级或快速兜底状态，使模型能够基于现有证据继续作答；用确定性传输层测试验证事件顺序和最终状态可见。

## 5. 完成部署、迁移和运维配置

- [x] 5.1 更新 compose、环境变量示例和配置文档，加入各层预算、候选上限、rerank 上限与索引读开关；用 compose 配置渲染和应用启动检查验证部署值有效。
- [x] 5.2 编写 expand/backfill/switch 发布步骤与回滚步骤，明确先部署双写、再回填校验、后启用索引读取；在测试数据库演练升级与回滚并确认数据不丢失。
- [x] 5.3 补充阶段状态、耗时、降级率、兜底率和超时率的观测说明与排障查询，确保 correlation ID 可贯穿 Pi 与 Hindsight；用一次端到端请求核对两侧日志可以关联。

## 6. 验证超时修复

- [x] 6.1 增加端到端故障注入测试，分别让分析模型、embedding、检索臂和 reranker 延迟或挂起，验证请求在设计总预算内结束并尽可能返回部分证据。
- [x] 6.2 增加客户端断开测试，验证 Pi、MCP 与 Hindsight 之间的取消会向下传播，且测试结束后没有未完成任务或继续产生的 provider 请求。
- [x] 6.3 在目标部署配置下分别运行正常查询和故意超时查询，记录 fast/deep 延迟、阶段耗时、降级结果与回答预留，确认不再出现工具和回合同时在 300 秒终止的竞态。
- [x] 6.4 运行受影响服务的格式检查、类型检查、单元测试与集成测试，并执行 `openspec validate stabilize-deep-search-deadlines --strict`；修复所有失败后保存验证结果。
