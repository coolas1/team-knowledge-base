## 1. Relevance gate (search-evidence spec)

- [x] 1.1 In `_filter_by_relevance` (`src/engine/hindsight_components/recall.py`), replace the `keyword_score > 0` bypass with the coverage floor: keyword-only candidates pass iff ≥ `recall_min_term_coverage` (default 0.5) of salient query terms (via `lexical_tokens()`) are present in the candidate text AND ≥ `recall_min_term_count` (default 2) terms match; semantic-floor and deep-rerank paths unchanged; single-term queries fall back to the semantic floor. Add both options to `hindsight_components/config.py`. Verify with unit tests (note: `src/engine/hindsight_components/tests` is not auto-collected — run explicitly): a one-term-of-seven match is filtered, a multi-term match passes, a semantically-matching keyword-less candidate passes, a relevant conversation memory passes the same gate, and the trace reports the filtered count.
- [x] 1.2 Allow fewer-than-`top_k` results end to end: confirm the MCP search tools return only gated sources and surface `filtered_count` in the trace. Verify with `tests/agent/test_mcp.py`: a mixed corpus (one matching doc, one matching conversation turn, one single-term match) returns exactly the two relevant sources.

## 2. Metadata-enriched retrieval views (search-evidence spec)

- [x] 2.1 Implement the retrieval-view helper (title | filename | overview[:300] + original text) and use it for chunk embeddings (`src/engine/graphrag/pipeline.py` / `components/embedder.py`) and for memory-unit embeddings and lexical tokens in the retain path (`hindsight_components/repository.py`); display text everywhere stays the original. Verify with unit tests: a noisy-body document whose title matches a query surfaces via recall, and its displayed/evidence text is the original extraction.
- [x] 2.2 Add a CLI backfill that recomputes retrieval views, re-embeds all chunks and current memories, and rebuilds lexical tokens for every current document (works from stored `chunk_text` + `documents.overview`; no original files needed). Verify with the integration suite or an isolated DB: after backfill, both 自动驾驶 papers (d4a6aa4b noisy-body doc in the fixture) surface for the title-matching query where they did not before.

## 3. Whole-response budget (knowledge-tools spec)

- [x] 3.1 Slim source metadata in `_source_from_candidate` (`query.py`): keep doc_id, title, source_type, session/turn ids, scores; drop the embedded document record. Make `based_on` opt-in via the existing `include` parameter (recall responses omit it; reflect keeps a compact form with no repeated evidence text). Cap `related_entities` by the new `engine_tools_entities_max` setting (default 10). Verify with `tests/engine/test_hindsight_query.py`: no document record in metadata, no `based_on` by default, `include` restores it, no evidence text twice in one response.
- [x] 3.2 Add `engine_tools_response_max_chars` (default 24,000) to `config/settings.py` and a shared MCP-layer budget helper in `src/agent/tkb/mcp/server.py` applied to `search`, `search_knowledge_fast`, `search_knowledge_deep`: serialize, over budget → drop lowest-ranked sources first, set `payload_trimmed`/kept/dropped in the trace. Document the knobs in `config/app.yaml` (comment-only). Verify with `tests/agent/test_mcp.py` forcing a tiny budget: the serialized response stays within it and reports trimming.

## 4. Verification

- [x] 4.1 Full gate: `uv run ruff check` and `uv run pytest` green; hindsight tests run explicitly (`uv run pytest src/engine/hindsight_components/tests`); `cd src/extensions/pi-agent && npm test` green if skill docs changed (update `skills/tkb-search/SKILL.md` et al. if they reference dropped response fields).
- [ ] 4.2 Post-release (LAN): run the backfill once, then replay the 2026-09-12 conversation (看看有哪些和自动驾驶相关的论文 + 总结第一篇文章) — both papers surface, no unrelated or single-term-match sources, conversation memories present only when relevant, each search response within the whole-response budget, both turns well under 300s; record evidence under `bench/`.
