---
id: 21
question_category: temporal
answer_type: precise
ground_truth_version: 1
---

# A21 — The R² change lab-notebook entry

## Answer

- **Entry date**: **2024-09-30 — Figure 2 regenerated**.
- **Old R²**: **0.71**
- **New R²**: **0.66**
- **Design change**: chart was updated to use **site-cluster colors**
  (three clusters: northern refugia, mid-latitude forereef, southern
  forereef) instead of a single colormap. Pooling across clusters in the
  old version was inflating the correlation; the cluster-aware trend line
  revealed a lower (and more honest) R².

## Sources

- `raw/research/lab-notebook.md` entry "2024-09-30 — Figure 2 regenerated":
  > "Updated `figures/temp-vs-bleach.png` to use the site-cluster colors
  > rather than a single colormap. The three clusters (northern refugia,
  > mid-latitude forereef, southern forereef) now read more clearly. R²
  > dropped from 0.71 to 0.66 with the cluster-aware trend line — that's
  > expected, since pooling across clusters was inflating the correlation."

## Reasoning

Direct quote lookup. The entry names both R² values and explains the
mechanism (cluster-aware fit removes the artificial inflation from
pooling sites with fundamentally different SST-bleach responses).

## Gotchas

- The current chart PNG (`figures/temp-vs-bleach.png`) only shows the
  **new** version with R² ≈ 0.66 (and the cluster legend). The old
  single-colormap version is not in the corpus — only referenced in
  the lab-notebook history.
- The chart's legend label formats R² as `R² = 0.66` (two decimal
  places). The lab-notebook prose uses the same `0.66`.

## Acceptable answer variants

- "2024-09-30 entry; 0.71 → 0.66; switched from single colormap to cluster-aware trend line"
- "Sep 30 2024 lab entry; R² dropped 0.71 to 0.66 because cluster pooling was inflating correlation"
- "Lab notebook 2024-09-30, R² went 0.71 → 0.66 with cluster colors"
