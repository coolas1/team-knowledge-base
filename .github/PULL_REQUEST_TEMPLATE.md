## 变更说明

<!-- 做了什么、为什么;一段话说清 -->

## openspec 变更

<!-- 对应的 openspec/changes/<name>;纯文档改动写"docs-only,无变更" -->

## 测试证据

<!-- 贴出 uv run ruff check / uv run pytest(及前端 npm test)结果 -->

---

提醒:PR 目标分支是 `develop`,需携带对应的 spec delta;推送前跑 lint + 测试。
