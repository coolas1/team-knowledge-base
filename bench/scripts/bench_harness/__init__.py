# bench_harness — shared library for the benchmark orchestrator scripts.
#
# Provides:
#   config.py     — load bench.yaml, resolve PKM entries
#   prompt.py     — build agent prompts from runbooks + input files
#   entrypoint.py — spawn an agentic CLI and collect its output
